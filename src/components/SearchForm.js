import React from "react";

const SearchForm = () => {

    return (
        <form id="searchForm">
            <label>Search for an artist or keyword:</label>
            <input type="searchInput"></input>
        </form>
    );
}

export default SearchForm;